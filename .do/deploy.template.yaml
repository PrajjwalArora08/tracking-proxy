spec:
 name: mixpanel-api-proxy
 services:
 - name: mixpanel-api-proxy
   git:
     branch: master
     repo_clone_url: https://github.com/PrajjwalArora08/tracking-proxy
   dockerfile_path: ./Dockerfile
   http_port: 80
